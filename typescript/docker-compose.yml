version: '3.7'

services:
  db:
    container_name: school-administration-system-mysql
    image: mysql:8.4
    command: --mysql-native-password=ON
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: school-administration-system
    ports:
      - "33306:3306"
    # volumes:
    #   - ./database:/docker-entrypoint-initdb.d:ro

  external:
    container_name: school-administration-system-external
    build:
      context: ../external
      dockerfile: external-system.Dockerfile
    image: school-administration-system-external:1.0
    environment:
      TZ: Asia/Singapore
      NODE_ENV: production
    ports:
      - "5000:5000"

  # app:
  #   container_name: school-administration-system-app
  #   build: .
  #   environment:
  #     - DB_HOST=db
  #     - DB_PORT=3306
  #     - NODE_ENV=development
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - db
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
